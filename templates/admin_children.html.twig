{% extends "admin.html.twig" %}
{% block title %} - Children{% endblock %}

{% block content %}

<p id="usersmanage">Manage Children<p>
<section class="container text-center">
    <button id="btnAddUser" class="btnAddUser" style="margin-top:0%; margin-bottom:2%;">ADD CHILD</button>
</section>
<div id="addUser-form-container">
</div> 

<div id="admin_showall">
    <table class="table user-list" id="tbList">
        <thead>
            <tr>
                <th><span>Child ID</span></th>
                <th><span>User(Parent) ID</span></th>
                <th><span>First Name</span></th>
                <th><span>Last Name</span></th>
                <th><span>Date of Birth</span></th>
                <th><span>Gender</span></th>
            </tr>
        </thead>
{% for children in children %}
        <tbody>
            <tr>
                <td>
                    <span>{{ children.childId }}</span>
                </td>
                <td>
                    <span>{{ children.userId }}</span>
                </td>
                <td>
                    <span>{{ children.firstName }}</span>
                </td>
                <td>
                    <span>{{ children.lastName }}</span>
                </td>
                <td>
                    <span>{{ children.DOB }}</span>
                </td>
                <td>
                    <span>{{ children.gender }}</span>
                </td>
                <td>
                    <span id="info">
                    <a href="/updatechild/{{ children.childId }}" class="updateChild"><i class="fa fa-edit fa-fw" style="color: #444;"></i></a>&nbsp;&nbsp;
                    <a href="#" class="deleteChild" data-target="#confirm-delete-modal" data-id="{{ booking.bookingId }}"><i class="fa fa-trash fa-fw" style="color: #444;"></i></a>
                    </span>
                </td>
            </tr>
        </tbody>
{% endfor %}

<div class="modal" id="confirm-delete-modal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <h5>Delete this child info?</h5>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-outline-success" id="confirm-delete-button">Confirm</button>
            </div>
        </div>
    </div>
</div>

    </table>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.min.js"></script>
<script>
    $(document).ready(function(){
        $("#btnAddUser").click(function() {
            $(this).hide();
            $.ajax({
            url: "/admin/addchild",
            success: function(data) {
                $("#addUser-form-container").html(data);
            }
            });
        }); 
        $(".updateBooking").on("click", function(event) {
            // prevent default form submission behavior
            event.preventDefault(); 
            var bookingId = $(this).attr("href").split('/')[2]; // extract bookingId from href attribute
            $.ajax({
                type: "POST",
                url: "/admin/updatebooking/" + bookingId,
                success: function() {
                    window.location.href = "/admin/updatebooking/" + bookingId; 
                },
                error: function(jqXHR, textStatus, errorThrown) {
                    // log any errors to the console
                    console.error(errorThrown);
                }
            });
        });
        $(".deleteChild").on("click", function(e) {
            e.preventDefault();
            var childId = $(this).data("id");
            var modalId = $(this).data("target");
            $(modalId).modal("show");
            $("#confirm-delete-button").on("click", function() { 
                $(modalId).modal("hide");
                $.ajax({
                    type: "DELETE",
                    url: "/admin/children/" + childId,
                    data: {childId: childId},
                    success: function() {
                        window.location.reload(); 
                    },
                    error: function(jqXHR, textStatus, errorThrown) {
                        console.error(errorThrown);
                    }
                });
            });
        });
    });
</script>


{% endblock content %}


